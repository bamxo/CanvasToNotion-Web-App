# Copy to frontend/.env.local (gitignored) to point `npm run dev` at a deployed
# backend instead of http://localhost:3000. Trailing slash is trimmed.
# Vite only exposes vars prefixed with VITE_. Restart the dev server after changing.

VITE_API_BASE=https://your-backend.vercel.app
