# Legacy Netlify Functions (not deployed)

Pre-Vercel Netlify Functions implementation, kept for reference during the migration. NOT deployed. The live backend is the Express app in `backend/public/` (see `backend/MIGRATION.md`). Safe to delete once the Vercel deployment is verified in production.
